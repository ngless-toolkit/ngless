ngless "1.6"

input = load_sample_from_yaml('list.yaml', sample='sample')

println(input.name())

trim = 3
input = preprocess(input) using |read|:
   read = read[trim:]
   if len(read) < 10:
       discard
write(input, ofile='output.fq')
