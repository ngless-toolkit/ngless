ngless '1.3'
import "example-cmd" version "0.0"
import "mocat" version "0.6"

input = load_mocat_sample('sample')

testing(input)

testing(input, verbose=true)

testing(input, verbose=true, testing={one})

testing(input, alternative=true, alternative2=true)

testing2(input)

