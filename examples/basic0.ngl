ngless 0.0
input = fastq('../Ngless-Mine/test.fq')
preprocess(input) using |read|:
    read = substrim(read, min_quality=26)
    if len(read) < 20:
        discard
write(input, ofile='filtered.fq', format={csv})
