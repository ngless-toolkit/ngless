ngless "0.0"
input = fastq('../samples/ce.fq')
mapped = map(input,reference='ce10')
annotated = annotate(mapped, mode="union", ambiguity={allow}, features=[{exon}])
counts = count(annotated, min=1)
write(counts, ofile="../samples/CountsResult.txt")